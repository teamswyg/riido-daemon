name: repo-executable-knowledge-coverage

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/repo-executable-knowledge-coverage.yml"
      - "README.md"
      - "AGENTS.md"
      - "NOTICE.md"
      - "docs/**"
      - "tools/knowledgecoverage/**"
      - "go.mod"
      - "go.sum"
  pull_request:
    paths:
      - ".github/workflows/repo-executable-knowledge-coverage.yml"
      - "README.md"
      - "AGENTS.md"
      - "NOTICE.md"
      - "docs/**"
      - "tools/knowledgecoverage/**"
      - "go.mod"
      - "go.sum"

jobs:
  repo-executable-knowledge-coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.3
      - uses: actions/setup-go@v6.4.0
        with:
          go-version-file: go.mod
          cache: false
      - name: Verify repository executable knowledge coverage
        run: |
          mkdir -p out
          go test ./tools/knowledgecoverage -count=1
          go run ./tools/knowledgecoverage \
            -manifest docs/executable-knowledge.riido.json \
            -check-doc \
            -evidence-out out/repo-executable-knowledge-coverage.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: repo-executable-knowledge-coverage
          path: out/repo-executable-knowledge-coverage.json
          if-no-files-found: error
