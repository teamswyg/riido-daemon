// Package jsontest provides small JSON contract helpers for tests.
//
// It does not own domain schemas, privacy surfaces, or server API contracts;
// callers compare its derived shape against their own SSOT artifact.
package jsontest
