{
  "schema_version": "riido-migration-page.v1",
  "loop_source": "docs/30-architecture/loop-engineering/migration-daemon-distribution-host-integration-docs.riido.json",
  "id": "daemon-runtime-session-backpressure",
  "title": "RIID-4572: Runtime / Session Backpressure",
  "generated_doc": "docs/migration/daemon/distribution-host-integration/runtime-session-backpressure.md",
  "back_title": "distribution-host-integration",
  "back_path": "../distribution-host-integration.md",
  "blocks": [
    {"kind": "paragraph", "text": "This slice closes the C4 runtime/session boundary work."},
    {"kind": "heading", "text": "Decisions"},
    {"kind": "bullets", "items": [
      "process stdout/stderr stream buffers are SSOT constants in `internal/process` and stay fixed at 64 chunks each",
      "session event/result buffers stay fixed at 256 events and 1 terminal result",
      "runtime actor mailbox defaults to 16 messages",
      "supervisor actor mailbox defaults to 64 messages",
      "provider runtime streams remain lossless bounded streams; full buffers block and propagate backpressure instead of dropping text/log/warning events",
      "`internal/agentbridge/session` remains a C4 internal submodel, not a separate bounded context"
    ]},
    {"kind": "paragraph", "text": "The slice adds focused public CI for default-size and no-drop backpressure gates."},
    {"kind": "paragraph", "text": "It does not add provider CLI dependencies, retry queues, EventIngestor/outbox durability, concrete provider adapter ownership, task DB/project/mwsd local API packages, packaging artifacts, private infra, secrets, or local machine state."}
  ]
}
