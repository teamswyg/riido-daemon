{
  "schema_version": "riido-migration-page.v1",
  "id": "daemon-codex-auth-full-access-taskdbplane-adapter",
  "title": "RIID-4683: Taskdbplane Local Task DB Adapter",
  "generated_doc": "docs/migration/daemon/codex-auth-full-access/taskdbplane-adapter.md",
  "back_title": "codex-auth-full-access",
  "back_path": "../codex-auth-full-access.md",
  "blocks": [
    {"kind": "paragraph", "text": "This slice moves the local task DB control-plane adapter."},
    {"kind": "heading", "text": "Moved Surfaces"},
    {"kind": "bullets", "items": [
      "`internal/taskdb`",
      "`internal/agentbridge/controlplane/taskdbplane`",
      "Provider-runtime, runtime-scheduling, locking, and daemon migration documentation",
      "Public CI for guarded task DB mutation, command-id idempotent replay, runtime registry sidecars, lease sidecars, fencing tokens, expired lease handoff, and taskdbplane claim/report black-box scenarios"
    ]},
    {"kind": "paragraph", "text": "`internal/taskdb` owns the public daemon copy of `riido-task-db.v1` JSON schema and guarded mutation rules. It persists transitions, deterministic validation evidence, and command receipts without importing private `riido_daemon` packages or workspace projection code."},
    {"kind": "paragraph", "text": "`internal/agentbridge/controlplane/taskdbplane` adapts the local JSON DB into `TaskSourcePort` and `TaskReporterPort`. It claims eligible `Queued` rows, records guarded C1 transitions, stores runtime registry / lease sidecars next to the task DB, and rejects progress/result reports without matching active lease metadata."},
    {"kind": "paragraph", "text": "This slice imports C1/C2/C3 domain types from `github.com/teamswyg/riido-contracts` and does not reintroduce private `riido_daemon` internal packages."},
    {"kind": "paragraph", "text": "It does not move project/mwsd sync, local API/socket, CLI commands, `controlplane/saasplane`, server HTTP transport, packaging artifacts, private infra, secrets, or local machine state."}
  ]
}
