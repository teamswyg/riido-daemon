{
  "schema_version": "riido-migration-page.v1",
  "loop_source": "docs/30-architecture/loop-engineering/migration-daemon-riidoapi-local-api-docs.riido.json",
  "id": "daemon-riidoapi-local-api-4686",
  "title": "RIID-4686 - mwsdbridge/Project Projection Sync Migration",
  "generated_doc": "docs/migration/daemon/riidoapi-local-api/4686-mwsdbridge-project-sync.md",
  "back_title": "riidoapi local API",
  "back_path": "../riidoapi-local-api.md",
  "blocks": [
    {"kind": "paragraph", "text": "This slice moves the public-safe mwsd workspace projection adapter."},
    {"kind": "bullets", "items": [
      "`internal/mwsdbridge`",
      "`internal/project`",
      "`riido mwsd snapshot|projection|sync|orchestration|projects|status`",
      "focused public CI for mwsd Unix-socket handshake, workspace projection, project state persistence, project-to-taskdb sync, CLI black-box sync, public boundary import checks, no-public-TCP listener checks, and stdlib-only Go dependency checks"
    ]},
    {"kind": "paragraph", "text": "`internal/mwsdbridge` is the anti-corruption layer for the local macmini-workspace daemon. It reads only the mwsd JSON socket contracts and must not parse macmini-workspace files directly."},
    {"kind": "paragraph", "text": "`internal/project` owns the deterministic `riido-workspace-projection.v1` and `riido-project-state.v1` files. Its task DB sync adapter is intentionally narrow: it projects mwsd-discovered document tasks into public `internal/taskdb` records and initial `TaskCreated` transitions, but guarded task mutation remains owned by `internal/taskdb`."},
    {"kind": "paragraph", "text": "The CLI remains a thin adapter for this slice. `riido mwsd sync` writes the project state file and updates `riido-task-db.v1` through the projection sync adapter."},
    {"kind": "paragraph", "text": "It does not start provider processes, talk to the SaaS server, open public TCP listeners, or bundle the mwsd daemon."},
    {"kind": "paragraph", "text": "This slice does not move full `riido daemon ...` process lifecycle commands, `controlplane/saasplane`, server HTTP transport, packaging artifacts, private infra, secrets, or local machine state."}
  ]
}
