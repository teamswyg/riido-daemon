{
  "schema_version": "riido-readme-page.v1",
  "loop_source": "docs/30-architecture/loop-engineering/readme-docs.riido.json",
  "id": "readme-run-and-smoke",
  "title": "README: Run And Smoke",
  "generated_doc": "docs/readme/run-and-smoke.md",
  "back_title": "README",
  "back_path": "../../README.md",
  "blocks": [
    {"kind": "paragraph", "text": "Install latest macOS/Linux release:"},
    {"kind": "code", "language": "bash", "code": "curl -fsSL https://raw.githubusercontent.com/teamswyg/riido-daemon/main/scripts/install-riido-daemon.sh | sh"},
    {"kind": "paragraph", "text": "Existing installs use the same script to update in place. Stop the daemon, reinstall, then check the version:"},
    {"kind": "code", "language": "bash", "code": "riido daemon stop --force || true\ncurl -fsSL https://raw.githubusercontent.com/teamswyg/riido-daemon/main/scripts/install-riido-daemon.sh | sh\nriido version\nriido daemon start"},
    {"kind": "paragraph", "text": "`latest` is resolved through the GitHub Releases API because public daemon release currently uses the pre-release channel instead of GitHub's stable `/releases/latest` endpoint."},
    {"kind": "paragraph", "text": "Install a specific release:"},
    {"kind": "code", "language": "bash", "code": "RIIDO_DAEMON_VERSION=v0.0.1 \\\ncurl -fsSL https://raw.githubusercontent.com/teamswyg/riido-daemon/main/scripts/install-riido-daemon.sh | sh"},
    {"kind": "paragraph", "text": "Desktop/MSIX launchers download the matching GitHub Release asset into app user data before launching it. Distribution details live in [`docs/30-architecture/release-artifacts.md`](../30-architecture/release-artifacts.md)."},
    {"kind": "code", "language": "bash", "code": "go run ./cmd/riido --help\ngo run ./cmd/riido bridge providers\ngo run ./cmd/riido daemon start --socket /tmp/riido-agentd.sock --pid-file /tmp/riido-agentd.pid --log-file /tmp/riido-agentd.log\ngo run ./cmd/riido daemon status --socket /tmp/riido-agentd.sock\ngo run ./cmd/riido daemon stop --socket /tmp/riido-agentd.sock --pid-file /tmp/riido-agentd.pid\ngo run ./cmd/riido daemon stop --socket /tmp/riido-agentd.sock --pid-file /tmp/riido-agentd.pid --force"},
    {"kind": "paragraph", "text": "`riido daemon ...` chooses its task source through Factor 12 env:"},
    {"kind": "bullets", "items": [
      "`RIIDO_TASK_QUEUE_DIR`",
      "`RIIDO_TASK_DB_SOURCE_PATH`",
      "`RIIDO_SAAS_URL` + `RIIDO_DEVICE_ID` + `RIIDO_DEVICE_SECRET`"
    ]},
    {"kind": "paragraph", "text": "Only one source should be selected as the production source."},
    {"kind": "paragraph", "text": "OpenClaw local model speed is owned by OpenClaw config. If OpenClaw uses a slow Ollama default, repair config if needed and switch it to a verified faster local model before QA. The provider integration test now tries `ollama/llama3.2:latest` first, then falls back to `ollama/qwen3:8b` and `ollama/qwen2.5-coder:1.5b`; it can still be pinned with `RIIDO_OPENCLAW_INTEGRATION_MODEL`:"},
    {"kind": "code", "language": "bash", "code": "openclaw doctor --fix\nopenclaw models set ollama/llama3.2:latest\nRIIDO_OPENCLAW_INTEGRATION_MODEL=ollama/llama3.2:latest \\\nAGENTBRIDGE_INTEGRATION=1 go test ./internal/provider/openclaw -race -count=1 -run TestIntegration -v"},
    {"kind": "paragraph", "text": "Run local acceptance verification before deployment. The runner keeps provider failures as evidence, verifies the daemon install/update script in a sandbox, calls the real development AI Agent API directly, renders a React contract lab, renders the dashboard, and optionally publishes to the private local QA evidence bucket. The product probe can recover the API token and workspace id from `.riido-local/private/riido-client-storage-state.json`, so the LaunchAgent does not need token env vars:"},
    {"kind": "code", "language": "bash", "code": "RIIDO_AI_AGENT_TOKEN=<token> \\\nRIIDO_E2E_WORKSPACE_ID=<workspace_id> \\\nRIIDO_LOCAL_QA_S3_PREFIX=s3://<private-local-qa-evidence-bucket>/daily \\\ngo run ./tools/localqarunner -run-product"},
    {"kind": "paragraph", "text": "For deployment-time local verification, add `-strict-coverage`. Daily LaunchAgent runs should usually omit it so partial evidence still publishes, but a release gate should fail when any provider/product/Figma/release row is not fully passed:"},
    {"kind": "code", "language": "bash", "code": "RIIDO_LOCAL_QA_S3_PREFIX=s3://<private-local-qa-evidence-bucket>/daily \\\ngo run ./tools/localqarunner -run-product -strict-coverage"},
    {"kind": "paragraph", "text": "Task multi-assignment, SSE replay, and thread-message probes need a development task plus a SaaS-connected daemon runtime binding. If `RIIDO_E2E_TASK_ID` is absent, the probe first tries to create a temporary development task through `development.api.riido.io`, runs the real AI Agent API flow against it, and records a cleanup scenario after deleting the task. When the token can read tasks but cannot create task fixtures, the probe falls back to a readable existing task, records `contract.task.fixture.fallback_existing`, marks `contract.task.fixture.create` as skipped with the 403 evidence preserved, and still verifies assignment/SSE/thread-message behavior. Agent ids are optional because the probe tries assignable candidates, prefers the same `runtime_kind` pair, and records `ai_agent_runtime_binding_required` repair evidence when SaaS has agents but no live daemon/runtime binding:"},
    {"kind": "code", "language": "bash", "code": "RIIDO_E2E_COMMENT_BODY='local QA follow-up' \\\nRIIDO_LOCAL_QA_S3_PREFIX=s3://<private-local-qa-evidence-bucket>/daily \\\ngo run ./tools/localqarunner -run-product -product-task-mutations"},
    {"kind": "paragraph", "text": "`teamswyg/riido-client` is not the implementation target for this harness and must not receive Codex/local-QA commits. The daemon-owned contract lab writes `.riido-local/contract-lab/index.html` to show frontend developers the exact endpoint order and identifier rules."},
    {"kind": "paragraph", "text": "Figma visual rows are backed by golden screenshot references captured from the source Figma nodes. The product acceptance tool verifies node identity, PNG dimensions, and SHA-256 before copying the reference into `.riido-local/screenshots/ai-agent-product-acceptance/` for the dashboard/S3 handoff. This proves the daemon-side Figma boundary is visible and stable; it does not claim riido-client pixel parity."},
    {"kind": "paragraph", "text": "The release row is backed by a sandboxed installer run. It seeds an old `riido` binary under a temporary `RIIDO_DAEMON_INSTALL_DIR`, serves a local fake GitHub release asset to the public install script, verifies checksum/install behavior, and executes the installed binary's `version` command. This proves the update path without modifying the developer's real install:"},
    {"kind": "code", "language": "bash", "code": "go run ./tools/localreleaseacceptance \\\n  -evidence-out .riido-local/evidence/local-release-acceptance.json"},
    {"kind": "paragraph", "text": "Install the macOS daily local acceptance schedule. This is a developer-local LaunchAgent, not CI. It runs once per day and uploads latest plus timestamped evidence when `RIIDO_LOCAL_QA_S3_PREFIX` is set:"},
    {"kind": "code", "language": "bash", "code": "RIIDO_LOCAL_QA_S3_PREFIX=s3://<private-local-qa-evidence-bucket>/daily \\\ngo run ./tools/localqaschedule -repo \"$(pwd)\" -hour 9 -minute 0 -install\nlaunchctl print gui/$(id -u)/io.riido.local-qa"},
    {"kind": "paragraph", "text": "Local artifacts are written under `.riido-local/evidence/` and `.riido-local/dashboard/`. The dashboard shows what passed, failed, skipped, remains not verified, when the evidence expires, and the macOS LaunchAgent label/time/log paths/command preview that prove the daily local E2E schedule is installed without token text."}
  ]
}
