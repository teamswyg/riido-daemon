{
  "schema_version": "riido-security-redaction-detail.v1",
  "id": "pattern-catalog",
  "title": "금지 패턴 카탈로그",
  "loop_source": "docs/30-architecture/loop-engineering/security-redaction.riido.json",
  "generated_doc": "docs/20-domain/security-redaction/pattern-catalog.md",
  "blocks": [
    {"kind": "paragraph", "text": "현재 C7 실행 카탈로그는 `internal/policy` 의 `secretRedactionPatterns` 가 구현한다. 패턴 ID 는 stable external evidence/audit 값이므로 이름 변경은 breaking-policy 다."},
    {"kind": "table", "columns": ["pattern ID", "의미"], "rows": [
      ["`aws-access-key`", "AWS access key (`AKIA...`, `ASIA...`)"],
      ["`gcp-api-key`", "GCP API key (`AIza...`)"],
      ["`github-token`", "GitHub classic/fine-grained token 계열"],
      ["`gitlab-token`", "GitLab personal/project token 계열"],
      ["`anthropic-api-key`", "Anthropic API key"],
      ["`openai-api-key`", "OpenAI API key"],
      ["`jwt`", "JWT 모양의 bearer token"],
      ["`pem-private-key`", "PEM private key header"],
      ["`basic-auth-url`", "`https://user:pass@host` 형태의 basic-auth URL"],
      ["`env-secret-assignment`", "env assignment 형태의 `*_TOKEN=...`, `*_SECRET=...`, `*_KEY=...` 값"]
    ]}
  ]
}
